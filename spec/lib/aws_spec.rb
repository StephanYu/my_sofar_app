require 'aws'

describe 'AWS' do 
  context 'JSON file' do 
    it 'successfully downloads a file' do 
      #create JSON file
      #AWS.download JSON file
      #expect(response).to be a JSON file
    end

    it 'returns a file with a specific property' do 
      
    end
  end
end
